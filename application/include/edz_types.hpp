#pragma once

#include <stddef.h>
#include <stdint.h>
#include <stdbool.h>

#include "helpers/result.hpp"

namespace edz {

    using titleid_t = u64;
    using userid_t = u128;

    using processid_t = u64;
    using buildid_t = u64;

    using addr_t = u64;

}